<IfModule mod_rewrite.c>
    RewriteEngine On

    # Si la petición ya busca algo que existe en /public, no hagas nada
    RewriteCond %{REQUEST_URI} !^/public/

    # Redirige todo lo demás a la carpeta public de forma limpia
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>